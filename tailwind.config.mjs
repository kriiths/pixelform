/** @type {import('tailwindcss').Config} */
const config = {
  // Tailwind v4 uses CSS-based configuration in globals.css
  // This file is optional but can be kept for IDE support
};
export default config;
